# Environment Variables for AI Resume Builder Backend

# JWT Secret Key (use a strong, random string in production)
JWT_SECRET=your_jwt_secret_key_here

# MongoDB Connection URI
# For local development: mongodb://localhost:27017/resume_builder
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/database_name
MONGO_URI=mongodb://localhost:27017/resume_builder

# Server Port (optional, defaults to 4000)
PORT=4000

# Node Environment
NODE_ENV=development

# CORS Origin (for production, set to your frontend domain)
CORS_ORIGIN=http://localhost:5173
